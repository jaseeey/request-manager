import requestManager, { RequestManager } from './request-manager.js';

export type { ActiveRequest, RequestIdentity } from './request-manager.js';
export { RequestManager };
export default requestManager;
